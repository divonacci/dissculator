import CalculatorContainer from "./calculatorComponents/CalculatorContainer";

const Main = (): JSX.Element => {
  return (
    <main>
      <CalculatorContainer />
    </main>
  );
};

export default Main;
